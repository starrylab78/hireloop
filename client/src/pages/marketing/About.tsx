export function AboutPage() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-20">
      <p className="eyebrow mb-3">About</p>
      <h1 className="font-serif text-4xl md:text-5xl">Hiring shouldn't take six tools</h1>
      <p className="mt-6 text-lg text-ink-soft">
        HireLoop is a job board built around one idea: the loop between a recruiter posting a role
        and a candidate accepting an offer should be short, visible, and honest about where things stand
        for everyone in it.
      </p>
      <p className="mt-4 text-ink-soft">
        Recruiters get a pipeline that doesn't require three other tabs open. Candidates get a feed that
        tells them, upfront, whether a role is worth their time. No dark patterns, no plan that hides a
        button you can't find — every limit is enforced the same way it's shown to you.
      </p>
      <p className="mt-4 text-ink-soft">
        This build is a demo/portfolio project, not a company with a headcount — but it's built to the
        standard of one.
      </p>
    </div>
  );
}
